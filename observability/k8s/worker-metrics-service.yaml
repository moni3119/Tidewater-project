apiVersion: v1
kind: Service
metadata:
  name: settle-worker-metrics
  namespace: tidewater
spec:
  selector:
    app: settle-worker
  ports:
    - name: metrics
      port: 8001
      targetPort: 8001
